import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.davidamiga.calateret',
    // appId: 'com.applint.ateret',
  appName: 'Cal Ateret Yosef',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;